"use client";

export default function HorizontalRule() {
  return (
    <div style={{
      height: '2px',
      width: '100%',
      background: 'var(--foreground20)',
      borderRadius: '2px',
    }} />
  )
}
